/// Max length for `listings.description` (must match CHECK + RPC).
const int kListingDescriptionMaxLength = 8000;

/// Max length for `listings.registration` (must match CHECK + RPC).
const int kListingRegistrationMaxLength = 200;
